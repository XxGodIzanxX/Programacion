#include <stdio.h>

int main() {

	//while
	/*int i = 0;
	int continuar = true;

	while (i <= 10 && (continuar == true))
	{
		printf("%d", i);
		printf("Quieres continuar? 0 no 1 si\n", i);
		scanf_s("%d", &continuar);
		i++;
	}
	
	int dinero = 0;
	int horas;
	const int precioHora = 20;
	while (dinero < 1000)
	{
		printf("Cuantas horas has trabajado?\n");
		scanf_s("%d", &horas);
		dinero += horas * precioHora;
		printf("Llevas %d euros\n", dinero);

	}
	*/
	//for

	/*
	for (int i = 0; i <= 10; i++)
	{
		printf("%d\n", i);
	}
	*/

}